export interface CreateArenaInput {
  name: string;
  location: string;
  capacity: number;
  description: string;
  availability: string;
  image_url: string;
  pricing_model: string;
  price: number;
}
